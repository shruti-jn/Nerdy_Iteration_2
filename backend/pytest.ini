[pytest]
asyncio_mode = auto
addopts = -m "not integration"
markers =
    integration: marks tests that require live API keys (run with '-m integration')
    contract: backend WebSocket/HTTP contract tests; no live providers (run with '-m contract')
