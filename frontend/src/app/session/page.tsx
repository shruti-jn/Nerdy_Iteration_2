/**
 * Main tutoring session page — WebRTC video, mic controls, transcript panel.
 * Pipeline stage: Frontend — renders avatar stream and manages WebSocket connection.
 */
export default function SessionPage() {
  return <div>Main tutoring session</div>;
}
