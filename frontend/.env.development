# Mock mode — enables UI testing without a running backend.
# All hooks (audio, WebSocket, WebRTC) simulate their behaviour locally.
# Set to "false" or remove this file when connecting to a real server.
VITE_MOCK=false
